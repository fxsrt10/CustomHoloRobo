using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Body : MonoBehaviour {
    enum Days { ShiningFighters, AerialBeauties, MetalGrapplers, LittleRaiders, StrikeVanishers, TrickFlyers, LightningSkyers };

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
